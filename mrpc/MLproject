name: mrpc

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      epochs: {type: int, default: 1}
      train_steps: {type: int, default: 10}
    command: "python train_hub_manual.py --epochs={epochs} --train_steps={train_steps}"
